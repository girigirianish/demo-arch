import { IsiDirective } from './isi.directive';

describe('IsiDirective', () => {
  it('should create an instance', () => {
    const directive = new IsiDirective();
    expect(directive).toBeTruthy();
  });
});
