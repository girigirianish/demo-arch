app-header([portalConfig]='portalPublicConfig.headerContent')
.container-fluid
  .main-container
    .content
      router-outlet
    app-isi([portalConfig]='portalPublicConfig')
app-footer('[portalVersion]'='portalVersion' '[portalConfig]'='portalPublicConfig')
